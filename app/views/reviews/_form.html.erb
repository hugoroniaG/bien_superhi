<%= simple_form_for @review, data: { turbo: false } do |f| %>
	<%= f.input :title %>
	<%= f.input :restaurant %>
	<%= f.input :address %>
	<%= f.input :photo %>
	<%= f.input :body %>
	<%= f.input :cuisine %>
	<%= f.input :price %>
	<%= f.input :ambiance %>
	
	<%= f.button :submit %>
<% end %>

